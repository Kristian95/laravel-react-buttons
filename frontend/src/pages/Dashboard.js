import React from 'react';
import ButtonGrid from '../components/ButtonGrid';

const Dashboard = () => {
    return (
        <div>
            <ButtonGrid />
        </div>
    );
};

export default Dashboard;
